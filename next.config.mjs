/** @type {import('next').NextConfig} */
const nextConfig = {
    
        images: {
            domains: ['images.unsplash.com','assets.aceternity.com','plus.unsplash.com','campusgrader.s3.us-east-1.amazonaws.com']
        }
};

export default nextConfig;
